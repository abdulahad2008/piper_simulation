"""Entry points. Run them as modules from the project root, e.g.

    python -m piper_rl.scripts.validate_model
    python -m piper_rl.scripts.scripted_demo --episodes 20
    python -m piper_rl.scripts.train --algo sac --timesteps 1500000
    python -m piper_rl.scripts.evaluate --model runs/<name>/best/best_model.zip
"""
